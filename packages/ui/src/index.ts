// Public API of the shared UI package — genuinely generic, business-free
// primitives (§2). Feature-specific components live inside their feature.
export { cn } from './lib/cn';
export { Button, type ButtonProps } from './components/Button';
export { Spinner, type SpinnerProps } from './components/Spinner';
