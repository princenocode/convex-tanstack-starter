// Re-export the shared Prettier preset so every tool (CLI, editor, lint-staged)
// resolves the same configuration from the repo root.
export { default } from '@convex-tanstack-starter/prettier-config';
