// Public API of the tasks feature (§2). Routes compose `TasksPanel`; everything
// else (api hooks, controller, presentational pieces) stays internal.
export { TasksPanel } from './components/TasksPanel';
